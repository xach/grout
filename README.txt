Grout is a library for sending authenticated requests to Google data
services.

It provides support for getting authentication data and using that
data to send authenticated requests.

It does not process the content of requests or responses.

